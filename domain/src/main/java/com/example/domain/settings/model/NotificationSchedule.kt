package com.example.domain.settings.model

/**
 * Enum for the notification schedule options.
 *
 */
enum class NotificationSchedule {
    /** Schedule daily. */
    DAILY,
    /** Schedule weekly. */
    WEEKLY,
    /** Schedule monthly. */
    MONTHLY
}